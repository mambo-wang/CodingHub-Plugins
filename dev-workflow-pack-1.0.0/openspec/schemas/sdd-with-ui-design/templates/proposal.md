## 为什么（Why）

<!-- 说明本次变更的动机。解决什么问题？为什么现在做？ -->

## 变更内容（What Changes）

<!-- 描述哪些内容会变化。明确说明新增能力、修改或删除。 -->

## 能力清单（Capabilities）

### 新增能力（New Capabilities）
<!-- 本次引入的新能力。将 <name> 替换为 kebab-case 标识符（如 user-auth、data-export、api-rate-limiting）。每个新能力对应 specs/<name>/spec.md -->
- `<name>`: <本次能力覆盖内容的简要描述>

### 修改能力（Modified Capabilities）
<!-- 需求（REQUIREMENTS）发生变化的现有能力（不仅是实现层面）。
     仅当规格级别的行为变化时列入。每个需要 delta 规格文件。
     使用 openspec/specs/ 中的现有规格名称。如无需求变更则留空。 -->
- `<existing-name>`: <哪些需求发生了变化>

## 影响范围（Impact）

<!-- 受影响的代码、API、依赖、系统 -->
